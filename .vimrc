syntax enable                   "syntax coloring
set background=dark             "color scheme
colorscheme solarized
set hlsearch                    "search is highlighted with different background color
hi Search guibg=Red             "highlighted background is going to be red?
filetype indent plugin on       "set auto-indent on

"tab behaviour
set tabstop=8                   "tab=8 spaces
set expandtab                   "tab is expanded into spaces
set softtabstop=8               "auto indent will indent to 8 spaces
set shiftwidth=8                "auto-indent shift width

"other useful settings copied from the Internet
set number                      "show line numbers
set ignorecase                  "make searches case-insensitive
